# Copy this file to .env.local and fill in your values.
# These are NOT used by the demo handlers yet — wire them up as you build out
# real API integration (see README).

# Pathumma model server (FastAPI / your own backend)
NEXT_PUBLIC_PATHUMMA_API_URL=http://localhost:8000

# Optional: server-side only
PATHUMMA_API_KEY=
